# Render Deployment Guide

Complete guide for deploying Book Management API to Render.com

## Quick Start

1. **Push to GitHub**:
   ```bash
   git add .
   git commit -m "Deploy to Render"
   git push origin main
   ```

2. **Deploy on Render**:
   - Go to [Render Dashboard](https://dashboard.render.com)
   - Click "New" → "Blueprint"
   - Connect your GitHub repository
   - Render auto-detects `render.yaml`
   - Set `GEMINI_API_KEY` in environment variables
   - Deploy!

3. **Run Migrations**:
   ```bash
   # In Render Shell
   alembic upgrade head
   ```

## Full Documentation

See: `/Users/admin/.gemini/antigravity/brain/.../render_deployment_guide.md`

Or check `DEPLOYMENT.md` in project root.

## Environment Variables

Required in Render dashboard:

- `GEMINI_API_KEY` - Your Google AI API key
- `JWT_SECRET_KEY` - Auto-generated by Render
- `DATABASE_URL` - Auto-set when database linked

## Verification

After deployment:
- Visit: `https://your-app.onrender.com/docs`
- Test: `curl https://your-app.onrender.com/`

## Support

- Logs: Render Dashboard → Your Service → Logs
- Issues: Check troubleshooting section in full guide
